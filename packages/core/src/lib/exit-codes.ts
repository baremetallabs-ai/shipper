// Exit code used by `shipper ship` when a desktop pause request halts it at a stage boundary.
export const PAUSED_EXIT_CODE = 75;

// Exit code used by `shipper ship` when a single-issue ship fails retriably.
export const RETRIABLE_FAILURE_EXIT_CODE = 76;
